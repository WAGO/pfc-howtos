## SECTION=wago_specific_dal

config KBUSMODBUSSLAVE
	bool
	default n
	prompt "Modbus-PFC-Slave"
	select LIBMODBUS
	help
	     Standalone Modbus slave which mimics the 750-352 coupler. IO-Terminals accessed via KBUS.
